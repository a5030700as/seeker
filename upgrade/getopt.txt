	了解: CURL库的使用
	了解：CJSON的使用 
	x表示可以进入目录 
	r表示可以读取目录  没有r命令 那么就是无法去执行 ls 命令了
	w代表可以修改目录 
	目录的存在是管理文件 是更高一层 不涉及文件的内容的 层次 
	对目录而言 起码要有 读和执行权限 基本上都是 rwxrwxr-x

	===================================================================================
	掩码是进程属性： 掩码 表示禁止 某个权限  因此  需要设置 umask0000才是 所传既所得 
	===========================================================================
	       #include <unistd.h>

       int getopt(int argc, char * const argv[],
                  const char *optstring);

       extern char *optarg;
       extern int optind, opterr, optopt;

       #include <getopt.h>

===============================================================
